# ClaudeCode Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-07-04

## Active Technologies

- TypeScript 5.x + Next.js 16 (App Router), Tailwind CSS 4.x, @supabase/supabase-js, @supabase/ssr, @react-pdf/renderer, Resend (email)

## Project Structure

```text
securionis-chantiers/
├── src/app/              # Pages et API routes (Next.js App Router)
│   ├── (auth)/           #   Login
│   ├── (dashboard)/      #   Dashboard, chantiers, admin
│   │   ├── dashboard/    #     Vue d'ensemble inspecteur (KPI, graphique NC)
│   │   ├── chantiers/    #     CRUD chantiers, visites, rapports
│   │   └── admin/        #     Points de contrôle, utilisateurs, entreprise
│   └── api/              #   Routes API (PDF, email, écarts, create-user, assistant, export)
├── src/components/       # Composants React (chantier, visite, ecart, admin, pdf, ui)
├── src/lib/              # Supabase clients, email, env, utilitaires
│   ├── offline/          #   IndexedDB store + sync (PWA offline)
│   ├── email/            #   Envoi de rapports (signature dynamique par entreprise)
│   └── utils/            #   Security, file-validation, constants, photo-compress, canvas-annotations, storage-path, storage-upload, document-sources, format
├── src/hooks/            # Hooks React (autosave offline, photo-upload, online-status)
├── src/types/            # Types TypeScript (database.ts — profiles, entreprises, audit_logs, etc.)
├── supabase/             # Migrations SQL (001-033) + seed.sql + config.toml
└── public/               # PWA: manifest.json, sw.js, icon.svg
```

## Key Architecture

- **Modèle multi-entreprise** : 1 entreprise → N inspecteurs (profiles.entreprise_id)
- **Sécurité env** : `src/lib/env.ts` — getters lazy, garde `requireServer()` côté client
- **PWA offline** : Service Worker (network-first pages, cache-first assets), IndexedDB local-first autosave, sync auto au retour réseau
- **Dashboard** : `/dashboard` — KPI personnalisés par inspecteur (RLS), graphique NC, chantiers urgents, liste visites du mois cliquable
- **Email** : Signature dynamique depuis données entreprise, envoi multi-destinataires en une requête Resend
- **Checklist visite** : Sélection de points dégroupée (liste plate), ajout de thèmes en cours de visite sans perte des points existants, recherche globale par mot-clé (catégories + thèmes) avec debounce
- **Données** : 26 catégories, 442 thèmes, 447 points de contrôle (migration 025, source Excel)

## Security

- **RLS** : Activée sur toutes les tables. Documents scopés par chantier. Themes/base_documentaire/point_controle_documents en lecture seule pour non-admins. Profiles bloqué sur modification rôle/entreprise_id.
- **Storage RLS** : Policies sur buckets `rapports` et `visite-photos` (suppression scopée par chantier/admin)
- **CSP** : Content-Security-Policy stricte dans `next.config.ts` (frame-ancestors none, connect-src Supabase, frame-src Supabase pour aperçu PDF)
- **Autorisation API** : `canAccessVisite()`, `canAccessChantier()` dans `src/lib/utils/security.ts` — vérification propriété sur toutes les routes API
- **SSRF** : `isAllowedSupabaseUrl()` — whitelist stricte hostname exact du projet Supabase
- **XSS** : `escapeHtml()` dans tous les templates email ; React échappe nativement côté client ; pas de SVG upload
- **Upload** : `src/lib/utils/file-validation.ts` — whitelist extensions, validation MIME, taille max (50 Mo docs, 10 Mo images)
- **Rate limiting** : In-memory (`src/lib/rate-limit.ts`) sur toutes les routes sensibles (IA, email, export, create-user)
- **Admin guard** : `src/app/(dashboard)/admin/layout.tsx` — Server Component qui redirige si non-admin
- **Audit** : Table `audit_logs` pour traçabilité des actions sensibles
- **Middleware** : Exact path match pour routes publiques (anti-bypass)

## Commands

```bash
cd securionis-chantiers
npm run dev          # Serveur de développement
npm run build        # Build production
npm run lint         # Linting ESLint
npx supabase db push # Appliquer les migrations
npx supabase db seed # Charger les données de référence
```

## Code Style

TypeScript 5.x: Follow standard conventions
- Composants : PascalCase, un fichier par composant
- Hooks : camelCase préfixé use-
- "use client" uniquement pour les composants interactifs
- Server Components par défaut pour les pages
- Min 44x44px pour tous les éléments tactiles (tablette)
- Variables d'env : toujours via `src/lib/env.ts`, jamais `process.env!` directement
- Sécurité : toujours via `src/lib/utils/security.ts` pour auth checks, `src/lib/utils/file-validation.ts` pour uploads

## Recent Changes

- 002-securionis-chantiers: Implemented full SST inspection app (66 source files, 11 migrations, 15 pages, build OK)
- 2026-03-22: Sécurisation clés API (env.ts), dashboard inspecteur, PWA offline (SW + IndexedDB), relation inspecteur-entreprise, email dynamique
- 2026-03-27: Audit sécurité complet — RLS renforcée (migrations 022-023), CSP, autorisation API, SSRF whitelist stricte, XSS emails, validation uploads, admin guard, Storage RLS, anti-énumération comptes, bloquer modification rôle, rate limiting, audit logging, prompt injection mitigation
- 2026-03-28: Modèle freemium (Stripe), aperçu PDF (CSP frame-src), envoi email multi-destinataires, copyright footer
- 2026-03-29: Import 447 points de contrôle depuis Excel (migrations 025-026), correction chargement catégories (retrait filtre phase_id IS NULL, fix RLS), dashboard visites du mois cliquable, sélection points dégroupée, ajout thèmes sans perte de points existants
- 2026-04-07: Recherche globale par mot-clé (catégories + thèmes) sur nouvelle visite et ajout en cours de visite, bouton "+ Catégories/Thèmes" déplacé en bas de checklist
- 2026-04-08: Audit sécurité complet v2 — trigger PostgreSQL anti-modification rôle (migration 027), autorisation écarts/statut + photos/analyze, fix bypass export XLSX, prompt injection mitigation (balises XML), Content-Length limits sur fetch, sender name sanitization, audit logging étendu (email envoi, écart statut), rate limiter anti-memory-exhaustion, validation scope export, upgrade Anthropic SDK
- 2026-04-15: Remarques générales visite (migration 028, champ textarea + section PDF), suppression visite en cours (API DELETE /api/visites/[id] + DeleteVisiteButton avec confirmation, audit log), bouton « Remarques » (orange) dans checklist (migration 029, valeur 'remarques' dans contrainte CHECK), section Remarques dans PDF (fond ambre/bordure orange), fix type TS valeur reponses
- 2026-04-28: Déploiement VPS Hostinger (31.97.36.92) — Docker Compose, Nginx reverse proxy, Cloudflare Flexible SSL. Fix NEXT_PUBLIC_APP_URL (https://ton-domaine.com → https://chantiers.securionis.com), rebuild container (résolution erreurs "Failed to find Server Action"), Supabase Auth URL Configuration mise à jour (Site URL + Redirect URLs). Commande de mise à jour : cd /app/securionis && docker compose up --build -d
- 2026-05-10: Refactoring 3 hotspots haute complexité (cyclo 55/35/26) en P0/P1/P2 — 12 nouveaux modules utils/composants, photo-annotator/point-controle-form/admin-documents passent de 1732 à 751 lignes (−57 %). renderAnnotations mutualisé (canvas-annotations.ts), helpers storage (storage-path.ts, storage-upload.ts), CategorieThemeSelector avec état discriminé, PointControleDocumentsUploader (forwardRef + flushPending), admin/documents découpé en 5 sous-composants + hook useDocuments, useReducer machine d'état canvas. Bugs RLS catégories (migration 033 admin INSERT/UPDATE/DELETE sur is_custom) + Resend silencieux (vérifier result.error sur envoi, RESEND_FROM_EMAIL → racine securionis.com car sous-domaines vérifiés séparément).
- 2026-05-11: Sélection des destinataires avant envoi du rapport — modal de cases à cocher dans rapport-actions.tsx (tous cochés par défaut, compteur, tout cocher/décocher), API /api/visites/[id]/email accepte `{ destinataireIds: string[] }` avec filtrage côté serveur anti-injection sur destinataires liés au chantier (rétro-compatible : body absent = envoi à tous).
- 2026-05-11 (salve 2): Infra qualité + features — CI GitHub Actions (.github/workflows/ci.yml, tsc+build à chaque push), ESLint 9 réparé (flat config eslint-config-next, npm script eslint . car next lint supprimé dans Next.js 16), historique d'envoi sur page rapport (EmailHistory lit audit_logs via serviceClient), email ad-hoc dans modal envoi rapport (extraEmails côté API avec validation regex + anti-injection), Sentry @sentry/nextjs 10 en mode no-op tant que DSN absent (sentry.{client,server,edge}.config.ts + instrumentation.ts + withSentryConfig), notifications push PWA MVP (migration 034 push_subscriptions, lib/push.ts avec cleanup 404/410, API /api/push/{subscribe,test}, hook use-push-notifications, page /dashboard/notifications, SW v5 avec handlers push+notificationclick — config VAPID requise). Bug fix : 3 inserts audit_logs utilisaient resource_type au lieu de resource (colonne réelle, migration 022), tous corrigés.
- 2026-06-07: Export des photos en ZIP pour les administrateurs (API /api/photos/export, jszip), nouvelle page d'accueil /admin en grille.
- 2026-06-19: Avertissement "Ne pas répondre à cet email" ajouté dans tous les emails sortants (rapports + documents).
- 2026-06-21: Refactoring dashboard (tableau Visites groupé par chantier + grille 2 colonnes avec NC en attente) et branding copyright FWN.
- 2026-07-04: Fix bandeau "NC corrigées" qui restait affiché après génération du rapport — comparaison de timestamps `ecarts.updated_at` vs `visites.updated_at` (désormais rafraîchis explicitement à chaque update, faute de trigger DB) via `isRapportAJour()` dans chantiers/[id]/page.tsx.
- 2026-07-04 (sécurité + infra): Mises à jour CVE (ws 8.21.0, next 16.2.10). Audit complet — durcissement applicatif (poweredByHeader off, rate-limits, magic-bytes uploads, buckets Storage allowed_mime_types+size), durcissement VPS (firewall UFW restreint aux IP Cloudflare, Docker bind 127.0.0.1:3000, SSH clé uniquement + fail2ban, Cloudflare Full Strict avec cert Let's Encrypt + renouvellement auto). Migration xlsx→exceljs (élimine vuln high SheetJS). CSP durcie (unsafe-eval retiré, object-src none) — nonce+strict-dynamic écarté car Turbopack ne nonce pas les scripts RSC inline. Buckets privés non appliqués (blocage logo email + migration données, risque réel faible car pas de listing anon). Date de visite ajoutée au bandeau NC corrigées de la page rapport.
- 2026-08-27: Familles + recherche full-text des points de contrôle (migrations 035-036) — colonnes `famille` (12 familles regroupant les 28 catégories) et `mots_cles` (text[]) sur `points_controle`, colonne générée `search_vector` + index GIN, configuration `french_unaccent` (recherche insensible aux accents et par préfixe). Page /admin/points-controle refondue : barre de recherche instantanée (debounce 250 ms) et filtres en cascade Famille → Catégorie → Thème → Statut. Miroirs applicatifs `src/lib/utils/familles.ts` et `mots-cles.ts` (aucun trigger DB : les écritures doivent renseigner `famille`/`mots_cles`). Correction du débordement de la nav dashboard en tablette (seuil barre/menu dépendant du nombre de liens du rôle : xl pour l'administrateur, md sinon).
- 2026-08-28 (audit sécurité v4): SSRF corrigé sur `photos/export` et `rapports/export` (URL de la base téléchargées sans `isAllowedSupabaseUrl()`, colonnes modifiables par un inspecteur via l'API REST). Dépendances : 7 vulnérabilités hautes → 0 (override `minimatch@3 > brace-expansion`). Cloisonnement multi-entreprise durci (`null !== null` rendait la vérification inopérante). `create-user` : upsert et journal d'audit désormais vérifiés. Rate limiting ajouté sur 7 routes. `VAPID_PRIVATE_KEY` via `env.ts`. Migrations 039-040 : `search_path` fixe et retrait de l'exposition RPC des fonctions SECURITY DEFINER (advisors Supabase 10 → 2). Les 3 profils rattachés à l'entreprise FWN — sans quoi `create-user` échouait et la gestion des utilisateurs aurait cessé de fonctionner.
- 2026-08-28 (plans PE/EXE): Marquage des documents de chantier en plan d'enquête publique (PE) ou plan d'exécution (EXE) — migration 041 (`plan_type`, `plan_version`, `parent_version_id` sur `documents`, appliquée), menu d'action par document avec numéro de version pré-rempli, badges vert #2E7D32 / orange #E67E22 et bandeaux « Plan PE de référence » / « Plan EXE V{n} ». `parent_version_id` chaîné côté application (aucun trigger). La page chantier gagne une vraie barre d'onglets `ChantierTabs` (Documents | Visites | Comparaison) — elle n'en avait pas, tout était empilé ; l'onglet Comparaison est un écran « Bientôt disponible ». Deux correctifs CSS issus de la vérification navigateur : `overflow-y-hidden` sur la barre d'onglets (`overflow-x-auto` bascule l'axe Y en auto) et `flex-wrap` sur la ligne d'actions d'un document, qui débordait en 375 px avec le 4e bouton.
- 2026-08-28 (comparaison PE/EXE): Page `/chantiers/[id]/comparaison` avec OpenSeadragon — superposition des plans PE et EXE dans un seul viewer (zoom et position partagés par construction, pas de synchronisation à maintenir), curseur d'opacité, vue côte à côte, inversion des calques, zoom/recadrage/plein écran. `pdfjs-dist` ajouté en plus d'`openseadragon` : tous les plans réels sont des PDF, qu'OpenSeadragon ne sait pas afficher — la page demandée est rendue dans un canvas puis fournie en URL blob, avec sélecteur de page pour les dossiers multi-pages. Vérifié sur les plans de production (dossier de 12 pages). `clickToZoom` désactivé (OpenSeadragon zoome au simple clic par défaut).
- 2026-08-28 (comparaison PE/EXE, suite): Panneau d'opacité repensé — deux opacités indépendantes (une par plan) au lieu d'une seule sur le calque du dessus, lecture en direct « Plan PE : x % — Plan EXE : y % », boutons −/+ au pas de 5 %, Reset (50/50), Basculer (PE seul ↔ EXE seul) et cinq pastilles de préréglage. Fond du visualiseur éclairci en conséquence (un plan à 50 % virait au gris sale sur fond sombre). Le bouton cadenas ne synchronise rien — la synchronisation est structurelle puisque les deux plans sont deux calques d'un même viewer — il sert au **recalage** : déverrouillé, le glissement déplace le seul calque du dessus (`preventDefaultAction` sur `canvas-drag`, décalage commité sur `canvas-drag-end`), avec bouton de recentrage. Compteur « Différences repérées » de session en haut à droite (+ et remise à zéro), sans détection automatique.
- 2026-08-28 (annotations de comparaison): Outils d'annotation sur la vue de comparaison — flèche, cercle, rectangle, texte, marqueur et main, quatre couleurs (critique/moyen/résolu/info), déplacement et redimensionnement des formes, liste filtrable par couleur avec commentaire et suppression, export JSON. Migration 042 : table `comparaisons` (la « session » = couple de plans et de pages, sous contrainte d'unicité — elle n'existait pas et servait d'ancrage aux annotations) et `comparaison_annotations`, RLS calquée sur celle des documents. Les coordonnées sont stockées en unités monde OpenSeadragon (le plan PE fait 1 de large), ce qui rend le repositionnement exact au rechargement quels que soient le zoom et l'écran. Rendu par une couche SVG unique dont le groupe porte le transform du viewport.
- 2026-08-28 (NC depuis une annotation): Bouton « Créer une NC » sur chaque annotation de la comparaison, modale pré-remplie (titre, description reprise du commentaire, type « Écart de plan », priorité déduite de la couleur, chantier, plans PE/EXE, capture PNG de la zone annotée générée depuis le canevas OpenSeadragon). Migration 043 : `ecarts.reponse_id` devient nullable — toute NC dérivait jusqu'ici d'une réponse de checklist, ce qu'une annotation de plan n'a pas — sous contrainte qu'une NC sans réponse soit de type `ecart_plan` ; ajout de `titre`, `type`, `priorite` et d'un `numero` d'identité (les 77 NC existantes numérotées) ; nouvelle table `comparaison_nc_links` (+ `capture_url`). Audit préalable : aucun consommateur (rapport, PDF, dashboard, export) ne casse. Nouvelle page de détail `/chantiers/[id]/nc/[ncId]` avec section « Plan comparé » et lien de retour vers la comparaison via `?pe=&exe=`. L'annotation liée s'affiche en bleu avec le badge « NC #n » sans que sa couleur de gravité soit modifiée en base. Piège : la capture va dans le bucket `visite-photos`, `rapports` étant restreint à application/pdf (400 sinon).
- 2026-08-28 (branding): Le pied de page passe de « FWN - Securionis » à « BTP-UP » — layouts dashboard et connexion, page de garde et pied du manuel Word, exemple du champ « Nom de l'entreprise ». Le nom de l'entreprise en base (table `entreprises`) n'est pas concerné : il se change depuis `/admin/entreprise`.
- 2026-08-28 (export de la comparaison): Menu « Exporter » (PNG / PDF), bouton « Imprimer » et partage par email sur la page de comparaison. La capture PNG est **composée à la main** (`src/lib/utils/comparaison-capture.ts`) : `html2canvas` est inutilisable ici, il ne sait pas analyser les couleurs `oklch()` de Tailwind v4 (le conteneur porte `bg-gray-100`) et rendrait à la résolution CSS. La vue n'étant que le canevas OpenSeadragon plus une couche SVG, les recomposer donne la résolution native. Nouvelles routes `POST /api/comparaisons/[id]/{pdf,email}` : elles ne reçoivent que l'image, **les annotations sont relues en base**. Rapport PDF en trois pages (garde, vue en A4 paysage, tableau des annotations avec total et signature) via `@react-pdf/renderer`, déjà présent — aucune dépendance ajoutée, aucune migration. Impression par `<iframe srcdoc>` masqué (`about:srcdoc` passe la CSP). Libellés d'annotation extraits dans `comparaison-libelles.ts`, le serveur ne pouvant pas importer un module « use client ». Piège react-pdf : une image a besoin d'une hauteur en points, `flexGrow: 1` déclenche « bigger than available page height ». Correction de doc au passage : `audit_logs` attend `details`, pas `metadata`.
- 2026-08-28 (rendu d'impression): Le bouton « Imprimer » produisait une page 1 quasi vide et une capture coupée entre les pages 2 et 3. Cause arithmétique : `img { width: 100%; height: auto }` sur 277 mm de large utiles et un ratio de capture 944/844 réclame 248 mm de haut pour 190 disponibles. Le document est refait dans `src/lib/utils/comparaison-impression.ts` — en-tête (chantier, plans PE/EXE colorés, nombre d'annotations, date), capture bornée sur **les deux** dimensions (`max-height: 168 mm`, rupture mesurée à 176 mm avec Chrome headless en-têtes activés), puis seconde page avec le tableau des annotations quand il y en a. `print-color-adjust: exact` sans quoi les pastilles de couleur disparaissent à l'impression. Résultat vérifié : 1 page sans annotation, 2 avec.
- 2026-08-28 (détection automatique des différences): OpenCV.js installé et pipeline de prétraitement en place — `src/lib/opencv.ts` (chargement paresseux + typage), `plan-preprocessing.ts` (gris, redimensionnement, CLAHE, alignement ORB+RANSAC) et `plan-diff-detection.ts` (absdiff, seuillage, morphologie, contours, classification `added`/`removed`/`modified`, filtrage). Bouton « Détecter les différences » dans la barre d'outils de la comparaison. **Le CDN est impossible** (`script-src 'self'`) : la bibliothèque est vendorisée dans `public/vendor/opencv`, préparée par un script reproductible qui en sort le binaire WebAssembly (9,8 Mo → 217 Ko de JS + 7,2 Mo de wasm) et neutralise deux des trois sites d'évaluation d'Embind. Le troisième, `craftInvokerFunction`, est irréductible : `'unsafe-eval'` est accordé **à la seule route de comparaison**, le reste de l'app gardant la CSP durcie. Deux correctifs indépendants : le middleware redirigeait `.wasm` vers `/login`, et le `.wasm` en `data:` URI était bloqué par `connect-src`. Piège majeur consigné : **le module OpenCV est un *thenable*** — `resolve(cv)` ou `return cv` depuis une fonction `async` provoque une adoption infinie qui fige l'onglet à 100 % de CPU sans erreur. Environ 400 ms par analyse. Aucune migration.
- 2026-08-28 (détection avancée + panneau): `detectStructuralDiffsAdvanced` (filtre bilatéral, seuillage adaptatif, carte de dissimilarité SSIM calculée à la main — OpenCV ne la fournit pas —, `SimpleBlobDetector` en appoint, confiance 0–1 pondérée sur quatre indices), `matchStructuralElements` (déplacé / redimensionné / ajouté / supprimé) et `estimateScale` (médiane des rapports de distances entre paires appariées, insensible aux faux appariements). Panneau latéral « Différences détectées » : tableau #, Type, Confiance, Surface, Aperçu, Action, triable et filtrable par confiance minimale ; les aperçus sont des vignettes avant/après découpées dans les images réellement comparées. Bouton « Annoter » qui reporte une différence en annotation `rect` sur la vue (conversion pixels → unités monde : diviser **les deux** axes par la largeur). Indicateur de progression à cinq étapes. Pas de bleu : la contrainte de couleur des annotations ne l'accepte pas et il est réservé aux NC. Pièges : `SimpleBlobDetector_Params` n'est pas constructible depuis JS (passer par `getParams()`/`setParams()`), et `convertTo(dst, type, alpha, beta)` ajoute une constante sans allouer de matrice `ones()`.
- 2026-08-28 (calque des écarts): Les différences détectées s'affichent sur le visualiseur — `DiffOverlay`, SVG en coordonnées monde OpenSeadragon dont le groupe porte le transform du viewport, donc synchronisé au zoom et au pan par construction. Rectangles semi-transparents (30 %, 60 % au survol), cliquables (ouvrent le panneau sur la bonne ligne), infobulle en `position: fixed` (type, confiance, surface, position). Légende en bas à droite. Barre de contrôle : afficher/masquer, opacité, quatre cases par type, confiance minimale, « Tout accepter » (une annotation par écart affiché) et « Tout rejeter » (écarte la détection, conserve les annotations déjà créées). Le seuil de confiance est **partagé** avec le panneau. Quatrième type `moved` produit par `matchTemplate` : une corrélation forte à une position décalée distingue un déplacement d'une modification, ce que ni `absdiff` ni le SSIM ne savent faire. Palette : vert #2E7D32, rouge #B41E1E, jaune #F59E0B, bleu #002855 — mais le bleu n'existant pas dans la contrainte des couleurs d'annotation, un écart `moved` s'enregistre en orange.
- 2026-08-28 (rapport de comparaison automatique): Bouton « Générer le rapport de comparaison » + modale (format PDF ou DOCX, envoi facultatif aux destinataires). Route `POST /api/comparaisons/[id]/rapport-auto` : elle reçoit du navigateur la carte des écarts, les miniatures des deux plans et la liste des écarts en JSON — la détection étant client-side — et relit tout le reste en base. Six pages : garde, résumé exécutif (répartition par priorité, NC créées, confiance moyenne), carte en A4 paysage avec légende, liste détaillée en paysage (8 colonnes dont priorité et recommandation), annexes avec les deux plans, puis annotations manuelles + historique des comparaisons + signature. Après génération : dépôt dans le bucket `rapports`, inscription dans les documents du chantier, journal `generate_comparaison_report`, envoi optionnel. ⚠️ La priorité et la recommandation sont une **règle déterministe** (`priorite-sst.ts`), pas une analyse SST — le rapport le dit en page de garde. Correctif au passage : la capture rasterise désormais toutes les couches SVG, le calque des écarts manquait aux exports PNG/PDF et à l'impression. DOCX via la dépendance `docx` déjà présente ; les deux formats partagent les mêmes données d'entrée.
- 2026-08-28 (types de fichiers du bucket `rapports`): Migration 044 — le bucket n'acceptait que `application/pdf` depuis le durcissement de juillet, alors que l'application autorise pdf/doc/docx/xls/xlsx/jpg/jpeg/png et écrit tout dans ce bucket. Les images et les documents Office étaient donc rejetés en 400 dans les documents de chantier et la base documentaire, et **l'upload du logo d'entreprise était cassé depuis juillet** (message d'erreur générique qui masquait la cause). Le bucket accepte désormais exactement les sept types de la whitelist applicative — pas au-delà de ce que le code valide déjà. Aucun changement de code : le correctif est côté configuration de stockage, donc actif en production sans déploiement.
